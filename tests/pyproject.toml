[project]
name = "tests"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "pytest>=8.0.0",
]

[tool.uv]
dev-dependencies = []

[tool.pytest.ini_options]
timeout = 60
